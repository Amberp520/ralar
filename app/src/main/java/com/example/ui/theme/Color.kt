package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// RALAR EXACT DESIGN SYSTEM PALETTE
// 70% Refined financial ledger + 20% modern digital product + 10% futuristic technology
val RalarPaper = Color(0xFFFBFAF7)     // --paper: Background canvas
val RalarSurface = Color(0xFFFFFFFF)   // --surface: Ledger cards / containers
val RalarInk = Color(0xFF16160F)       // --ink: Primary typography and marks
val RalarMuted = Color(0xFF6B6B5F)     // --muted: Secondary metadata & labels
val RalarLine = Color(0xFFE3E0D6)      // --line: Grid borders and ledger rules
val RalarAccent = Color(0xFF0E5C3A)    // --accent: Deep authoritative green (PAID, Verified)
val RalarPending = Color(0xFF8A6D1F)   // --pending: Warm ochre for PROCESSING state
val RalarFail = Color(0xFF9B2C21)      // --fail: Precision vermilion for FAILED state

// Subtle states and surface tints
val RalarAccentSurface = Color(0xFFF0F6F2)
val RalarPendingSurface = Color(0xFFFBF7EE)
val RalarFailSurface = Color(0xFFFDF2F1)
